# Runtime dependencies. Floors match the features the code actually uses;
# ceilings guard against breaking major/minor releases:
#   - streamlit is pinned to 1.59.x because the UI styles internal DOM
#     test-ids that can change between minor releases.
#   - crewai 1.x is the tested API generation.
# CI uploads the exact Linux-resolved versions as an artifact per run.
crewai>=1.15,<2
crewai-tools>=1.15,<2
streamlit>=1.59,<1.60
python-dotenv>=1.0,<2
filelock>=3.13,<4
